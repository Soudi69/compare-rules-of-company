import { Star, Compass, Rocket, Satellite, XCircle } from 'lucide-react'

// Themed chat icons for the Cosmos project
export const CosmosChatIcons = {
  chat: Star, // Main chat icon (replaces MessageCircle)
  analysis: Compass, // replacing something?
  send: Rocket, // Send button (replaces Send)
  clear: Compass, // Clear chat (replaces X)
  close: XCircle, // Close chat (replaces X)
  message: Satellite, // Message bubble (optional)
}
